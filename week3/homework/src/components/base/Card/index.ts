import { CardDescription, CardRoot, CardTitle, CardValue } from './Card';

export { CardDescription, CardRoot, CardTitle, CardValue } from './Card';

export const Card = {
  Root: CardRoot,
  Title: CardTitle,
  Value: CardValue,
  Description: CardDescription,
};
